# ========================================================
# POS Terminal Authentication Credentials (Server-side Only)
# ========================================================
ADMIN_PASSCODE=your_admin_passcode_here
STAFF_PASSCODE=your_staff_passcode_here

# ========================================================
# Neon PostgreSQL Database Configuration (Server-side Only)
# ========================================================
DATABASE_URL=postgresql://[user]:[password]@[neon-hostname]/[database]?sslmode=require
DIRECT_URL=postgresql://[user]:[password]@[neon-hostname]/[database]?sslmode=require
